# Minimal runtime dependencies for inference + web UI.
# Keeps installation easier on Windows by avoiding optional / hard-to-build packages.

# Core ML
torch>=1.7.0
torchvision>=0.8.1
torchaudio>=0.7.0

# Core numerics / data
# Torch builds commonly used here are not yet fully compatible with NumPy 2.x on Windows.
numpy>=1.19.5,<2
pandas>=1.2.0

# Config
pyyaml>=5.3.1

# Images / video
opencv-python>=3.4.2
pillow>=8.1.0
scikit-image>=0.18.1

# Modeling utils
timm>=0.3.4
facenet-pytorch>=2.5.1
pytorch-msssim>=0.2.1

# Metrics / helpers
scikit-learn>=0.24.0
tqdm>=4.55.1
tabulate>=0.9.0

# Download helper (for weights)
gdown>=3.12.2

# Optional convenience
imutils>=0.5.3
